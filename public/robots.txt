# robots.txt fuer flaechenklar.de
# Alle Crawler duerfen alles indexieren — kein Login, keine sensiblen Pfade.
# Hinweis: Cloudflare injiziert hier zusaetzlich einen "Cloudflare Managed
# Content"-Block mit User-agent-Direktiven und einer Content-Signal-Zeile.
# Wir definieren deshalb hier KEINEN eigenen User-agent-Block — sonst
# meldet Lighthouse "robots.txt ist ungueltig" (doppelte User-agent: *).
# Bleibt nur der Sitemap-Verweis, den jede robots.txt haben darf.

Sitemap: https://flaechenklar.de/sitemap.xml
